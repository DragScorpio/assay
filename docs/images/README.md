# docs/images/

Screenshots used in the project README. PNGs are treated as binary (see `.gitattributes`).

- `assay-nvda.png` — `assay NVDA`, a full triangulated report on real data.
- `assay-spcx.png` — `assay SPCX` (SpaceX), the honest-refusal case: a market price exists but there are no fundamentals to value, so every method declines.
