"""The optional Streamlit UI. Run with ``assay-ui`` or ``streamlit run assay/ui/app.py``.

The UI is a thin view over the same engine the CLI uses (see :mod:`assay.pipeline`). All numbers are
still computed deterministically; the sidebar sliders only change the forward-looking assumptions.
"""
